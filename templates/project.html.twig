{% extends 'partials/base.html.twig' %}

  {% block content %}
  <div class="project-container">

    <div class="project-info">
      <div class="footer-push">
        <h1 class="client">{{ page.header.client }}</h1>
        <h2 class="title">{{ page.title }}</h2>

        {{ page.header.description | markdown   }}
        
        
        {% if page.header.related_work %}
          <ul>
            <h3>Related Work</h3>
            {% for p in page.header.related_work %}
              <li><a href="{{ base_url ~ p.url }}">{{ p.title }}</a></li>
            {% endfor %}
          </ul>
        {% endif %}
      </div>

      <footer class="lg-footer">
        <p>
          <a href="mailto:hello@itsleewilliams.com">hello@itsleewilliams.com</a><br />
          © Lee Williams 2016<br />
          Developed by <a href="http://neilspurgeon.com/">Neil Spurgeon</a>
        </p>
      </footer>

    </div>
    
    <div class="horizontal-scroll">
      {#% set img_count = 0 %#}

      <section class="images">  
        {{ page.content }}


        {#{% for image in page.media.images %}
          <!-- Eclude thumbnail -->
          {% if image.filename != 'thumbnail.jpg' and image.filename != 'thumbnail.jpeg' and image.filename != 'thumbnail.png' %}        
            {% set img_count = img_count + 1 %}
            <a class="project-img" id="{{ 'image-' ~ img_count }}" rel="lightbox" href="{{ image.url }}">
                {{ image.html() }}
            </a>
          {% endif %}

        {% endfor %} #}

        <!-- Pagination -->
        {#{% if page.nextSibling.url %}
<!--             <div class="project-img project-img-end">
              <a href="{{ page.nextSibling.url }}">{{ page.nextSibling.title }}</a>
            </div> -->
        {% else %}
        <div class="project-img project-img-end">
<!--           <h1>Thanks for looking!</h1>
          <p>Feel free to contact me at 530 318 4337 or hello@itsleewilliams.com</p>
          <p>or visit my <a href="/info">about me</a> section if you'd like to know some more personal details.</p>
        </div> -->
        {% endif %}#}

      </section>

      <div class="pagination">
        {#% for i in 1.. img_count %}
          <a href="{{ '#image-' ~ i }}">{{ i }}</a>
        {% endfor %#}
      </div>

    </div>

    <footer class="sm-footer">
      <p>
        <a href="mailto:hello@itsleewilliams.com">hello@itsleewilliams.com</a><br />
        © Lee Williams 2016<br />
        Developed by <a href="http://neilspurgeon.com/">Neil Spurgeon</a>
      </p>
    </footer>


</div>
  {% endblock %}
