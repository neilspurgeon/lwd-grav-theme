<div class="nav-bg">
  <nav class="main-nav-container">
    <a class="left-nav" href="/">
      <h1 class="site-title">{{ site.title }}</h1>
    </a>

    <ul class="right-nav">
      {% for p in pages.children %}
      <li>
        <a href="{{p.url}}">
          <h2>{{ p.title }}</h2>
        </a>
      </li>
      {% endfor %}
    </ul>
  </nav>
</div>

<script>
 $(document).ready(function() {
  path = window.location.pathname;
  currentLink = $('nav a[href$="' + path + '"]');

  console.log(currentLink)

  if (currentLink[0]) {
    // add active to current page
    console.log('// add active to current page')
    currentLink.addClass('active');
  } else {
    // add active to work
    console.log('// add active to work')
    $('a[href$="/lee_williams_portfolio"]').addClass('active')
    console.log('else')
  }

 })
</script>